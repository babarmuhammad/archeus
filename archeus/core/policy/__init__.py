"""Policy, autonomy and authorisation (P9, p9-design-gate): the pure parts.

`rules` (the floor, the profiles, the predicates) and `engine` (the
evaluation). The commands that record, approve and check are the application
layer's (application/authorization.py); `worker` expires approvals.
"""
