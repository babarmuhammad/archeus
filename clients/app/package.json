{
  "name": "archeus-app",
  "private": true,
  "version": "0.0.0",
  "description": "The Archeus V1 SPA (P3.5b walking skeleton: Now and Work). Built into archeus/api/static; Node is a build-time dependency only.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --noEmit -p . && vite build",
    "typecheck": "tsc --noEmit -p ."
  },
  "dependencies": {
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "typescript": "5.9.3",
    "vite": "8.3.1"
  }
}
